services:
  stirling-pdf:
    image: frooodle/s-pdf:latest
    container_name: stirling_pdf_app
    ports:
      - "8089:8080"
    environment:
      - DOCKER_ENABLE_SECURITY=false
      - INSTALL_BOOK_AND_ADVANCED_HTML_OPS=true
      - LANGS=en_GB,id_ID
      - SYSTEM_CORSALLOWEDORIGINS=*
      # ──────────────────────────────────────────────────
    volumes:
      - ../../../app-storages/stirling/trainingData:/usr/share/tessdata
      - ../../../app-storages/stirling/extraConfigs:/configs
      - ../../../app-storages/stirling/logs:/logs
    restart: always